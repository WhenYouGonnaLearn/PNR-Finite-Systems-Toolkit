[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "pnr12-multitool"
version = "1.3.0"
description = "Python toolkit for finite mappings, state updates, and saved workspaces"
readme = "README.md"
requires-python = ">=3.11"
license = {text = "MIT"}
dependencies = []

[tool.setuptools]
py-modules = ["pnr12"]
