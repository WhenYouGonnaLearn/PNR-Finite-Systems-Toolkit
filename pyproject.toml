[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "pnr-fst"
version = "1.3.0"
description = "Python toolkit for explicit finite systems and saved workspaces"
readme = "README.md"
requires-python = ">=3.11"
license = {text = "MIT"}
dependencies = []

[tool.setuptools]
py-modules = ["pnr12"]
